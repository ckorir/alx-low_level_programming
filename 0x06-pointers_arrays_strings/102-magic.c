#include <stdio.h>

/**
 * main - Add one line to this code
 * Return: Nothing
 */
int main(void)
{
	int n;
	int a[s];
	int *p;

	a[2] = 1024;
	p = &n;
	*(p + 5) = 98;
	printf("a[2] = %d\n", a[2]);
	return (0);
}


